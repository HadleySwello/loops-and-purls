<div>
  <p data-id="{{dataId}}">{{patternName}}</p>
  <p>{{paragraph}}</p>
  <button type="button" class="patternDeleteButton btn-default" data-id="{{dataId}}" name="deleteButton">Delete</button>
  <form id="update-title" data-id="{{dataId}}">
    <input name="title" type="text" placeholder="Update Title"/>
    <input type="submit" class="btn-default" value="Submit">
  </form>
  <br></br>
</div>
